<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Some thoughts on business understanding</title>
      <link href="/2026/08/03/biz-understanding/"/>
      <url>/2026/08/03/biz-understanding/</url>
      
        <content type="html"><![CDATA[<div align="center">    <img src="/images/biz_understanding/image1.png" width="70%" /></div><blockquote><p>I can still remember when I interviewed and asked a question like “How do you understand the project in a bussiness level?”, many candidates would answer with the specific business proceduer of the project. I think that’s correct, but it’s only the basic standard though. </p><p>Today I’m trying to think more about the question and share some of my thoughts about what business understanding is. And TBH, I’d experienced a huge twist towards it.</p></blockquote><h1 id="Tech-stack-is-not-business-understanding"><a href="#Tech-stack-is-not-business-understanding" class="headerlink" title="Tech stack is not business understanding"></a>Tech stack is not business understanding</h1><p>In storage and low-level networking, the words we say most often are <code>high throughput</code>, <code>low latency</code>, and <code>zero-copy</code>. Yeah, these’re the basic targets in the field.</p><p>But try answering a different question. What real problem does the system you run solve for the business?</p><p>(Stop here, try spending a minute thinking about this question.)</p><p>Most engineers I know answer with more tech. “I wrote a custom bypass stack on DPDK and got latency down to 10μs.” “I built a sharding and metadata layer on RocksDB and pushed throughput up 30%.”</p><p>Those are good answers to a different question. They describe technical experience. They are not business understanding, and the gap between the two is where a lot of careers quietly stop to move forward.</p><p>I used to think people in infrastructure got a pass on this, which means they just need to nail the <code>tech solutions</code>. We work on the layer underneath everything, so surely the business stuff is someone else’s job. </p><p><strong>As long as my tech is solid, I’m fine. That turned out to be wrong</strong>.</p><p><strong>However, tech solutions are not that solid. Because they could be deprecated, they could be replaced by AI, and they could go to a wrong direction without enough understanding of business.</strong></p><p>The architecture and the protocols exist to serve the business. If you don’t know what the business needs, you are guessing.</p><h2 style="color:#2E86AB">What "business" means when you work below the application</h2><p>For an application developer, the business is orders, payments, risk control. For us it looks different. Our business is I&#x2F;O patterns, workload shape, and hardware bottlenecks.</p><p>There is a version of this you can reach fairly early. You know which services call your RPC framework. You know roughly how many QPS your storage cluster takes on a normal day. When someone asks, you can add an endpoint, change a config, tune a parameter. That is competent, and it is worth having.</p><p><strong>The more valuable version is knowing what actually hurts in each scenario. And this is what can help you stand out.</strong></p><p>In object storage, the pain is high performance metadata lookups, cost-efficient storage engine and high available user access.</p><p>In high-performance RPC, it is the pattern our user would integrate us, like what the threading model is, which underlying transport(userspace TCP&#x2F; RDMA &#x2F; kernel TCP) they choose from and why, and what the SOP is in order to handler various types of failure regarding their business scene.</p><p>Here is the part that matters for your career. If you switch companies, the specific RPC library you wrote might be thrown away on day one. The judgment about which hardware and which protocol fit which I&#x2F;O pattern goes with you. <strong>That is the asset. You carry it in your head, not in the repo.</strong></p><h2 style="color:#2E86AB">From running tickets to making the call</h2><p>Plenty of engineers understand the mechanism. You know a write goes through the WAL, lands in the memtable, and eventually flushes to disk. Call that level one. It is necessary and it is not enough.</p><p><strong>Level two is knowing the tradeoffs well enough to make a call. And this’s why tradeoffs matter in a system design interview, which is necessary for a senoir role.</strong></p><p>Take DPDK as an example.</p><ul><li><strong>Level one</strong> is knowing how to use <code>mbufs</code>, set up <code>rte_rings</code>, and write a small user-space stack.</li><li><strong>Level two</strong> is knowing that kernel bypass is not a free win. Poll mode pins a core at 100% whether or not traffic is flowing. On bursty or uneven workloads you can burn a whole CPU doing nothing. You know when DPDK earns its cost, when io_uring is the optimal choice, and when putting eBPF in front to drop junk packets early beats both. (💡 Note: Now DPDK has a hybrid mode to allow interruption and avoid polling during low-traffic time.)</li></ul><p>Storage is the same. Random small writes love a WAL in front of an LSM-tree. That pattern is popular and battle-tested for a reason. Large sequential reads and writes are quite different, and going through the <code>page cache</code> is often just overhead. <code>Direct I/O</code>, or <code>NVMe-oF</code> if you are crossing the network, tends to spend far less CPU per byte moved.</p><p>The day you can stand in a design review and say “here is how this pattern is usually handled, and here is the option that wins under our hardware and our workload,” you stop being the person who takes storage tickets. You become the person who decides how the thing gets built.</p><h2 style="color:#2E86AB">What if you are stuck on routine work right now</h2><p>I can hear the objection. “I maintain one small storage module and I spend my days fixing bugs. Where am I supposed to find this grand industry view?”</p><p>Fair. There is no shortcut, but there is a path.</p><ol><li><p>First, make your own patch of ground solid. Distributed systems and C++ punish surprises. A low crash rate, memory leaks you fix fast, and changes that don’t quietly regress performance are the price of admission. Nobody hands you a core component until they trust you with the small one.</p></li><li><p>Second, look past your own few thousand lines of code. Don’t only stare at the RPC handler or the storage engine you own. Watch how the services upstream call you. Could their retry logic turn a small failure into a avalanche against your cluster? Follow the data across clients, gateways, and storage nodes and see where it really goes. Trust me, this is also a shortcut to understand any data-oriented systems.</p></li><li><p>Third, build the whole map in your head. Once you have it, a new bottleneck stops being a mystery. You don’t open a flamegraph on your one module. You trace the map and say something like “this smells like cross-node NUMA access, or lock contention on the metadata path.” A sentence like that in a review lands right on the problem, and people notice. <strong>That said, building the whole map isn’t an overnight thing, we are supposed to start from a simple one, then thinking about related issues we faced, then add branches to the trunk</strong>.</p></li></ol><h2 style="color:#2E86AB">The part that sets your ceiling</h2><p>The further you get, the more everyone’s C++ starts to look the same. The locking tricks, the kernel knobs, the syntax. Everybody has read the same posts.</p><p>What separates people at that point is not how many C++20 features you can name. It is how fast you can find the real bottleneck in a system nobody fully understands, and how well you pick the fix.</p><blockquote><p>The tech is the tool in your hand. The understanding of the actual scenario, the domain and where it hurts, is the thing you stand on. So, we might as well stop creating projects by AI or being nervous about we haven’t mastered. Let’s slow down and think about what we’ve done.</p></blockquote><h2 style="color:#2E86AB">Accessment table</h2>Here's a simple accessment table for bussiness understanding. Take your time and think about each dimension.🤗To be honest, after reflecting, I found I still have room for improvement in several areas.<table><thead><tr><th>Dimension</th><th>Entry</th><th>Competent</th><th>Strong</th></tr></thead><tbody><tr><td>Business scope</td><td>You only understand the module you own.</td><td>You understand your team’s full business line.</td><td>You know all the company’s core business lines and how they interact.</td></tr><tr><td>Business depth</td><td>You can read a spec and build to it.</td><td>You understand the full flow of the core business, including how errors are handled.</td><td>You know the best practice at each step and can judge whether an approach is any good.</td></tr><tr><td>Industry awareness</td><td>You only know how your own company does it.</td><td>You know other companies in the industry are solving similar problems.</td><td>You understand the core pain points across the industry and the common solutions to them.</td></tr><tr><td>Requirements review</td><td>You only look at the parts that touch your module.</td><td>You can spot logic holes in a requirement.</td><td>You can quickly see how a requirement affects the whole business flow, and where it might conflict.</td></tr><tr><td>Technical decisions</td><td>You implement features from the product doc.</td><td>You can pick the right technical approach for the business scenario.</td><td>You can anticipate how the architecture should evolve based on where the business is heading.</td></tr></tbody></table>]]></content>
      
      
      <categories>
          
          <category> Everyday Reflection </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>A quick look at consistent hashing</title>
      <link href="/2026/07/30/consistent-hash-history/"/>
      <url>/2026/07/30/consistent-hash-history/</url>
      
        <content type="html"><![CDATA[<blockquote><p>Picture this. Your app is doing great. Traffic keeps climbing, so you do the sensible thing and grow your cache cluster from 10 servers to 11.<br>One extra machine. That should be harmless.</p><p>Then the dashboards go red. Cache hit rate falls off a cliff, basically to zero. Millions of queries that used to be served from memory now slam straight into your database. The database buckles, and the whole system goes dark.</p></blockquote><blockquote><p>What happened? You just triggered a cache avalanche. And the thing that caused it is the very first tool most of us reach for when we split data across servers: modulo hashing.</p><p>Here is the story of how a simple bit of arithmetic falls apart at scale, and how a group of MIT researchers fixed it with an idea that still runs under half the internet today.</p></blockquote><h2 id="The-late-90s-and-a-world-full-of-avalanches"><a href="#The-late-90s-and-a-world-full-of-avalanches" class="headerlink" title="The late 90s, and a world full of avalanches"></a>The late 90s, and a world full of avalanches</h2><p>Rewind to the late 1990s. The web was having its first real growth burst. A single server could no longer keep up, so distributed caching became the default: spread your key-value pairs across a group of servers, and let the client figure out which server holds what.</p><p>The most natural way to route a key to a server was modulo hashing:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">server_index = hash(key) % N</span><br></pre></td></tr></table></figure><p>where <code>N</code> is the number of servers.</p><p>It is beautifully simple. The distribution is even, it costs almost nothing to compute, <strong>and as long as <code>N</code> never changes, it just works</strong>.</p><p>But out in the real world, server fleets are never still. Machines crash. New ones get added. Maintenance happens. And modulo hashing has one fatal flaw: <strong>the moment <code>N</code> changes, almost every key maps to a different machine</strong>.</p><h3 id="Why-modulo-hashing-fails-so-badly"><a href="#Why-modulo-hashing-fails-so-badly" class="headerlink" title="Why modulo hashing fails so badly"></a>Why modulo hashing fails so badly</h3><p>The reason is right there in the math.</p><p>Say you have 10 servers and you scale up to 11. Roughly 90 percent of your keys now land on a different server than before.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">10 servers: hash(key) % 10  -&gt;  server A</span><br><span class="line">11 servers: hash(key) % 11  -&gt;  server B   (true for ~90% of keys)</span><br></pre></td></tr></table></figure><p>This is not a bug in your code. It is just arithmetic. The denominator <code>N</code> is baked into every single lookup, so when <code>N</code> moves, it drags almost the entire keyspace with it. One small change, and your cache is effectively empty overnight.</p><p>People tried to work around it. Over-provision servers so you rarely scale. Double the cluster size in one go to limit the shuffle. Re-index during the quiet hours. All of these are band-aids. None of them touch the real problem, which is that the server count and the data mapping are welded together.</p><p>A young company called Akamai ran straight into this wall while trying to solve content delivery. So they took the problem to some theoretical computer science researchers at MIT.</p><h2 id="The-breakthrough-put-keys-and-servers-on-a-ring"><a href="#The-breakthrough-put-keys-and-servers-on-a-ring" class="headerlink" title="The breakthrough: put keys and servers on a ring"></a>The breakthrough: put keys and servers on a ring</h2><p>In 1997, a team at MIT published a paper called Consistent Hashing and Random Trees.</p><p>Their key insight was almost cheeky: stop using <code>N</code> in the lookup at all.</p><p>Instead, take both your servers and your keys and map them onto the same fixed-size ring, usually the range from 0 to 2^32 minus 1. Imagine a giant clock face.</p><div align="center">    <img src="/images/consistent_hash_his/image1.png" width="90%" /></div><p>Here is the whole trick in three steps:</p><ol><li>Hash each server’s identifier (its IP, for example) and drop it onto the ring.</li><li>Hash each key with the same function so it lands somewhere on the ring too.</li><li>To find a key, start at its spot and walk clockwise until you hit the first server. That server owns the key.</li></ol><h3 id="Why-this-is-such-a-big-deal"><a href="#Why-this-is-such-a-big-deal" class="headerlink" title="Why this is such a big deal"></a>Why this is such a big deal</h3><p>When you add or remove a server, the only keys that move are the ones sitting between the new server and the one before it on the ring. Everything else stays exactly where it was.</p><p>The math backs this up. With <code>N</code> servers, adding one more only disturbs about one Nth of your keys.</p><ul><li>In a 10 node cluster, adding a node moves about 10 percent of keys, versus 90 percent with modulo.</li><li>In a 100 node cluster, adding a node moves about 1 percent.</li></ul><p>Notice the direction of that trend. The bigger your cluster gets, the less a scaling operation hurts. That is the opposite of how modulo behaves, and it is the whole point.</p><h2 id="Fixing-uneven-load-with-virtual-nodes"><a href="#Fixing-uneven-load-with-virtual-nodes" class="headerlink" title="Fixing uneven load with virtual nodes"></a>Fixing uneven load with virtual nodes</h2><p>The basic ring has one weakness. If your hash function happens to place a few servers close together, one unlucky server can end up owning a huge arc of the ring, and it takes on way more load than the rest.</p><p>The paper’s fix is virtual nodes.</p><p>Instead of giving each physical server one point on the ring, you give it many virtual identities, say <code>server1-1</code>, <code>server1-2</code>, <code>server1-3</code>, and so on.</p><div align="center">    <img src="/images/consistent_hash_his/image2.png" width="90%" /></div><p>Scatter those virtual nodes all over the ring, and the load evens out across your fleet. It is a classic systems move: use lots of small random samples to get a smooth, balanced result.</p><h2 id="What-it-looks-like-under-the-hood"><a href="#What-it-looks-like-under-the-hood" class="headerlink" title="What it looks like under the hood"></a>What it looks like under the hood</h2><p>You do not need much code to see how this works. Finding a key on the ring is really just a ceiling search: given the key’s hash, find the smallest node hash that is greater than or equal to it. If you run off the end, wrap back to the first node.</p><p>In Java or Go you would usually keep the virtual node hashes in something sorted, like a TreeMap, a red-black tree, or a skip list. Here is the idea in Go using a sorted slice and a binary search:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Ring <span class="keyword">struct</span> &#123;</span><br><span class="line">    hashes []<span class="type">uint32</span>          <span class="comment">// sorted virtual node hashes</span></span><br><span class="line">    nodes  <span class="keyword">map</span>[<span class="type">uint32</span>]<span class="type">string</span> <span class="comment">// hash -&gt; physical node name</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(r *Ring)</span></span> Get(key <span class="type">string</span>) <span class="type">string</span> &#123;</span><br><span class="line">    h := hash(key)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// find the first vnode hash &gt;= h (the clockwise neighbour)</span></span><br><span class="line">    i := sort.Search(<span class="built_in">len</span>(r.hashes), <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> r.hashes[i] &gt;= h</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// walked past the end of the ring? wrap around to the start</span></span><br><span class="line">    <span class="keyword">if</span> i == <span class="built_in">len</span>(r.hashes) &#123;</span><br><span class="line">        i = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> r.nodes[r.hashes[i]]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>That <code>sort.Search</code> call is the clockwise walk. The wrap-around <code>if</code> is what turns a plain line into a ring.</p><h3 id="The-Redis-twist-hash-slots"><a href="#The-Redis-twist-hash-slots" class="headerlink" title="The Redis twist: hash slots"></a>The Redis twist: hash slots</h3><p>It is worth seeing how a modern tool adapted the idea. Redis Cluster does not use a continuous ring at all. It uses 16,384 fixed hash slots.</p><p>Keys are hashed with <code>hash(key) % 16384</code> to pick a slot, and slots are explicitly assigned to nodes. You can think of this as a pragmatic, simplified consistent hashing: a fixed, bounded lookup space, with slot migration that is easy for an operations team to reason about and move around by hand.</p><div align="center">    <img src="/images/consistent_hash_his/image3.png" width="90%" /></div><h2 id="The-system-design-lessons-underneath"><a href="#The-system-design-lessons-underneath" class="headerlink" title="The system design lessons underneath"></a>The system design lessons underneath</h2><p>Consistent hashing is more than a caching trick. It quietly demonstrates a few principles worth carrying into other designs:</p><ol><li>Keep volatile state off the hot path. The current node count changes all the time, so do not bake it into every lookup.</li><li>Make change cheap. The cost of a change should scale with the size of the change, not the size of the whole system.</li><li>Keep the blast radius small. When something scales or fails, contain the fallout to a predictable, local slice.</li></ol><h2 id="Where-it-shows-up-today"><a href="#Where-it-shows-up-today" class="headerlink" title="Where it shows up today"></a>Where it shows up today</h2><p>The paper went from a niche academic result to a foundation of distributed infrastructure:</p><ul><li>DynamoDB and Apache Cassandra use consistent hashing rings to partition data across storage nodes.</li><li>Memcached clients use it to spread cache entries across independent instances, with no central coordinator.</li><li>Akamai’s CDN still leans on these ideas to route billions of requests to edge servers.</li></ul><h2 id="Are-there-better-options-now"><a href="#Are-there-better-options-now" class="headerlink" title="Are there better options now?"></a>Are there better options now?</h2><p>Consistent hashing set the bar, but people never stop tuning.</p><ul><li>Jump Consistent Hash (Google, 2014) is fast and needs zero memory, no stored ring, no virtual nodes. It is only a few lines of code and it flies. The catch is that it only supports adding or removing nodes at the tail end of the cluster.</li><li>Rendezvous hashing (also called highest random weight) scores every key-node pair and picks the highest scorer. It gets you incremental rebalancing without any ring or virtual nodes at all.</li></ul><p>In today’s cloud-native world, a lot of traffic routing is handled higher up by service meshes and ingress controllers. But go down into the engine room, where database sharding, distributed caches, and storage partitions live, and the principle is exactly the same as it was in 1997: scaling should not break the world.</p>]]></content>
      
      
      <categories>
          
          <category> Back to basics </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Back to basics </tag>
            
            <tag> System Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Cache Aside, and the Consistency Problems Nobody Warns You About</title>
      <link href="/2026/07/24/db-cache-policy/"/>
      <url>/2026/07/24/db-cache-policy/</url>
      
        <content type="html"><![CDATA[<blockquote><p>Putting Redis in front of MySQL is the easiest performance win in backend work. It is also the fastest way to create a new kind of bug: two copies of the truth that disagree.</p><p>This post walks through the Cache Aside pattern, the races it opens up, and which fixes actually work. Examples are in C++.</p></blockquote><h2 id="The-whiteboard-and-the-filing-cabinet"><a href="#The-whiteboard-and-the-filing-cabinet" class="headerlink" title="The whiteboard and the filing cabinet"></a>The whiteboard and the filing cabinet</h2><p>Here is the picture I keep in my head.</p><p>Your database is a filing cabinet. Everything is in there. It is organised, it is correct, and it is slow. Walking over to it takes time.</p><p>Your cache is a whiteboard on the wall next to your desk. It holds a handful of things you looked up recently. Reading it takes a glance.</p><p>Two rules follow from this, and almost everything else in the post is a consequence of them.</p><p><strong>The filing cabinet is the truth.</strong> The whiteboard is a convenience.</p><p><strong>When the two disagree, you wipe the whiteboard.</strong> You never try to fix it up. You erase it and let the next person copy the answer down fresh.</p><p>For the code below, assume these interfaces:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Cache</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Cache</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> std::optional&lt;std::string&gt; <span class="title">get</span><span class="params">(std::string_view key)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">set</span><span class="params">(std::string_view key, std::string_view value,</span></span></span><br><span class="line"><span class="params"><span class="function">                     std::chrono::seconds ttl)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">del</span><span class="params">(std::string_view key)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Database</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Database</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> std::optional&lt;User&gt; <span class="title">selectUser</span><span class="params">(<span class="type">int64_t</span> id)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">updateUser</span><span class="params">(<span class="type">const</span> User&amp; u)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> std::string <span class="title">userKey</span><span class="params">(<span class="type">int64_t</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;user:&quot;</span> + std::<span class="built_in">to_string</span>(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="1-Reading"><a href="#1-Reading" class="headerlink" title="1. Reading"></a>1. Reading</h2><p>Cache Aside means your application does the managing. The whiteboard does not know about the filing cabinet. The filing cabinet does not know about the whiteboard. Your code is the only thing that connects them.</p><p>To read something:</p><ol><li>Glance at the whiteboard.</li><li>It is there? Take it. That is a <strong>hit</strong>.</li><li>It is not there? That is a <strong>miss</strong>. Walk to the cabinet, get it, copy it onto the whiteboard, then use it.</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">std::optional&lt;User&gt; <span class="title">UserRepo::getUser</span><span class="params">(<span class="type">int64_t</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="keyword">auto</span> key = <span class="built_in">userKey</span>(id);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">auto</span> cached = cache_.<span class="built_in">get</span>(key)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">deserialize</span>&lt;User&gt;(*cached);          <span class="comment">// hit</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> user = db_.<span class="built_in">selectUser</span>(id);                 <span class="comment">// miss, go to the cabinet</span></span><br><span class="line">    <span class="keyword">if</span> (!user) &#123;</span><br><span class="line">        <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cache_.<span class="built_in">set</span>(key, <span class="built_in">serialize</span>(*user), kDefaultTtl);</span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>People call this <em>lazy loading</em>. The name fits. Nothing gets written on the board until somebody asks for it. So every key misses once, right after startup. Usually that is fine.</p><p>One thing worth building in early. If the whiteboard falls off the wall, you should still be able to work. A cache outage should slow you down, not stop you. Wrap those cache calls so a failure logs and falls through to the database. Most Redis clients throw by default, and a throw here turns a cache problem into an outage.</p><h2 id="2-Writing-and-why-you-erase"><a href="#2-Writing-and-why-you-erase" class="headerlink" title="2. Writing, and why you erase"></a>2. Writing, and why you erase</h2><p>To write something:</p><ol><li>Update the filing cabinet.</li><li><strong>Erase</strong> that entry from the whiteboard.</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UserRepo::updateUser</span><span class="params">(<span class="type">const</span> User&amp; u)</span> </span>&#123;</span><br><span class="line">    db_.<span class="built_in">updateUser</span>(u);              <span class="comment">// the truth first</span></span><br><span class="line">    cache_.<span class="built_in">del</span>(<span class="built_in">userKey</span>(u.id));      <span class="comment">// then wipe the copy</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Two small decisions there. Both matter more than they look.</p><h3 id="Why-erase-instead-of-rewrite"><a href="#Why-erase-instead-of-rewrite" class="headerlink" title="Why erase instead of rewrite?"></a>Why erase instead of rewrite?</h3><p>The obvious alternative is to write the new value onto the board. That breaks the moment two people write at once.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Thread A: update DB (v1) ─────────────────────────► set cache (v1)</span><br><span class="line">Thread B:        update DB (v2) ──► set cache (v2)</span><br><span class="line">                                                     ▲</span><br><span class="line">                                        board says v1, cabinet says v2</span><br></pre></td></tr></table></figure><p>A filed its change first but scribbled on the board last. Now the board is wrong, and it stays wrong. Nobody will notice until the entry expires.</p><p>Erasing does not have this problem. Two people erasing the same spot leaves the same result as one person erasing it. Order stops mattering. Whoever reads next walks to the cabinet and copies down whatever settled there.</p><p>There is a second reason. Some values are expensive to work out — a big join, an aggregate, a rendered page. If you rewrite the board on every change, you pay that cost even for entries nobody reads. If you erase, you pay it when somebody actually asks. And if nobody asks, you never pay at all.</p><h3 id="Why-the-cabinet-first"><a href="#Why-the-cabinet-first" class="headerlink" title="Why the cabinet first?"></a>Why the cabinet first?</h3><p>Flip the order and the failure gets a lot worse.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Thread A (write): erase board ─────────────────► update DB</span><br><span class="line">Thread B (read):     miss → read DB (old) → write board (old)</span><br><span class="line">                                                    ▲</span><br><span class="line">                                     stale value, now stuck on the board</span><br></pre></td></tr></table></figure><p>A reader slipped into the gap. It found the board blank, went to the cabinet, and copied down the old value — because the new one was not filed yet. That stale entry now sits there until it expires. Could be an hour.</p><p>The other order has a race too. We will get to it. But it is a much smaller one.</p><p>So: <strong>file it, then wipe the copy.</strong></p><h3 id="One-trap-with-transactions"><a href="#One-trap-with-transactions" class="headerlink" title="One trap with transactions"></a>One trap with transactions</h3><p>If the write is inside a transaction, erase <strong>after the commit</strong>. Not inside it.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UserRepo::updateUser</span><span class="params">(<span class="type">const</span> User&amp; u)</span> </span>&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span> txn = db_.<span class="built_in">begin</span>();</span><br><span class="line">        txn.<span class="built_in">updateUser</span>(u);</span><br><span class="line">        txn.<span class="built_in">commit</span>();               <span class="comment">// must land before we touch the cache</span></span><br><span class="line">    &#125;</span><br><span class="line">    cache_.<span class="built_in">del</span>(<span class="built_in">userKey</span>(u.id));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Erase before the commit and a reader can copy down a version of the world that does not include your change. You have just recreated the erase-first bug without meaning to.</p><p>This one sneaks in easily. The invalidation ends up in an ORM hook, or in a repository method that somebody calls halfway through a transaction, and nothing looks wrong.</p><h2 id="3-The-race-that-is-left"><a href="#3-The-race-that-is-left" class="headerlink" title="3. The race that is left"></a>3. The race that is left</h2><p>Even in the right order, there is still a gap.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Thread A (read):  miss → read DB (old) ──────────────────► write board (old)</span><br><span class="line">Thread B (write):            update DB (new) → erase board</span><br><span class="line">                                                              ▲</span><br><span class="line">                                        stale value written after the erase</span><br></pre></td></tr></table></figure><p>A reader misses, walks to the cabinet, picks up the old value — and then stalls. While it is standing there, B files a change and wipes the board. A comes back, does not know any of that happened, and writes its stale value onto a board that was just cleaned.</p><p>Cabinet says new. Board says old. Nothing will fix it on its own.</p><p><strong>How often does this really happen?</strong> Less often than the diagrams suggest. It needs all three of these at once:</p><ul><li>a miss on that exact key, and</li><li>the reader stalling between the cabinet and the board, and</li><li>that stall lasting longer than an entire write</li></ul><p>Reads are usually much faster than writes. So the reader has to get descheduled at exactly the wrong moment, on a key that just missed, that somebody is writing right now. It does happen. At enough traffic everything happens. But plenty of systems look at those odds and decide to live with it.</p><p>Which brings us to the dullest fix in the post, and the best one.</p><h3 id="Always-set-a-TTL"><a href="#Always-set-a-TTL" class="headerlink" title="Always set a TTL"></a>Always set a TTL</h3><p>Everything else here shrinks the window. A TTL puts a <strong>ceiling</strong> on it.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">constexpr</span> <span class="keyword">auto</span> kDefaultTtl = std::chrono::<span class="built_in">minutes</span>(<span class="number">30</span>);</span><br></pre></td></tr></table></figure><p>Think of it as wiping the whole board every so often, whether it needs it or not. It costs you a few extra trips to the cabinet. In exchange, every bug in this post has a time limit.</p><p>That covers the race above, a delete that failed, a key you forgot to invalidate, and the invalidation bug you have not found yet. Without a TTL, one dropped delete means wrong forever. With one, it means wrong for half an hour.</p><p>An entry with no expiry is a bet that your invalidation code is perfect. It is not.</p><h2 id="4-Delayed-double-delete"><a href="#4-Delayed-double-delete" class="headerlink" title="4. Delayed double delete"></a>4. Delayed double delete</h2><p>This is the usual fix for the race above. Erase the entry, wait a moment, erase it again. The second wipe catches anything a slow reader scribbled down in between.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">UserRepo::updateUserDoubleDelete</span><span class="params">(<span class="type">const</span> User&amp; u)</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="keyword">auto</span> key = <span class="built_in">userKey</span>(u.id);</span><br><span class="line"></span><br><span class="line">    db_.<span class="built_in">updateUser</span>(u);</span><br><span class="line">    cache_.<span class="built_in">del</span>(key);                        <span class="comment">// first wipe</span></span><br><span class="line"></span><br><span class="line">    scheduler_.<span class="built_in">after</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">500</span>), [<span class="keyword">this</span>, key] &#123;</span><br><span class="line">        cache_.<span class="built_in">del</span>(key);                    <span class="comment">// second wipe, later, async</span></span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The wait has to be asynchronous. Blocking a request thread in <code>sleep_for</code> to serve a write is a good way to run out of threads.</p><p>Now, be honest about what this buys you. It makes a stale entry less likely. It does not make it impossible.</p><p>The delay needs to outlast the slowest reader in the gap. You cannot know that number. It moves with load, with GC pauses, with a bad network moment. 500ms is a number people copy from each other, not one anybody derived.</p><p>And it does nothing for the failure that actually shows up in production: <strong>the erase itself failing</strong>. If Redis is down, or the process dies right after the commit, both wipes are lost. Sending two requests to a service that is not answering does not help.</p><p>Worth knowing. Worth being able to explain. But it is a probability adjustment wearing the costume of a guarantee.</p><h2 id="5-Binlog-and-MQ-the-real-fix"><a href="#5-Binlog-and-MQ-the-real-fix" class="headerlink" title="5. Binlog and MQ: the real fix"></a>5. Binlog and MQ: the real fix</h2><p>Here is the underlying problem with all of the above. The wipe lives in application code, in the same process that just did the write. So it can be lost the same way anything in that process can be lost.</p><p>Move it out. Let the <strong>filing cabinet announce its own changes</strong>.</p><p>Every cabinet already keeps a logbook. In MySQL it is the binlog, and it is how replication works. Tools like <a href="https://github.com/alibaba/canal">Canal</a> or <a href="https://debezium.io/">Debezium</a> pretend to be a replica, read that logbook, and publish what they see onto a queue. Something on the other end reads the queue and wipes the matching entries.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">App ──update──► MySQL ──binlog──► Canal/Debezium ──► Kafka ──► consumer ──DEL──► Redis</span><br></pre></td></tr></table></figure><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// The application no longer touches the cache when it writes.</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">InvalidationConsumer::onRowChange</span><span class="params">(<span class="type">const</span> RowChangeEvent&amp; ev)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (ev.table != <span class="string">&quot;users&quot;</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="keyword">auto</span> id = ev.after.<span class="built_in">at</span>(<span class="string">&quot;id&quot;</span>).<span class="built_in">asInt64</span>();</span><br><span class="line">    cache_.<span class="built_in">del</span>(<span class="built_in">userKey</span>(id));         <span class="comment">// idempotent, safe to replay</span></span><br><span class="line">    <span class="built_in">ack</span>(ev.offset);                  <span class="comment">// only ack once the wipe worked</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Why this is better:</p><ul><li><strong>It survives a crash.</strong> The logbook entry is written as part of the commit. If your app dies a millisecond later, the wipe still happens.</li><li><strong>Retries are free.</strong> Erasing is idempotent, so at-least-once delivery is exactly the guarantee you want. Do not ack until it works, and redelivery costs nothing.</li><li><strong>Writers stop having to care.</strong> Every service that writes to that table gets correct invalidation. Including the ones you did not write. Including the DBA running a manual <code>UPDATE</code> at 2am.</li><li><strong>Order is kept.</strong> Partition the topic by primary key and changes to one row arrive in the order they were committed.</li></ul><p>It is not free. That is another system to run, and consumer lag becomes a thing you have to watch. The wipe is now <strong>eventually consistent</strong> — milliseconds to seconds, and longer if the consumer falls behind.</p><p>One requirement: <code>binlog_format = ROW</code>. Statement-based logging does not tell you which rows changed.</p><p>My honest advice. Start with cabinet-first, then wipe, plus a TTL. Move to the logbook approach when either the staleness starts causing real incidents, or when the number of services writing to your tables gets bigger than the number you can keep in your head.</p><h2 id="6-The-other-write-patterns"><a href="#6-The-other-write-patterns" class="headerlink" title="6. The other write patterns"></a>6. The other write patterns</h2><p>Cache Aside is the default. It is not the only option.</p><h3 id="Write-Through"><a href="#Write-Through" class="headerlink" title="Write Through"></a>Write Through</h3><p>Write to the board, and the board writes through to the cabinet for you, right then. One place to call, and the two never disagree.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">WriteThroughRepo::updateUser</span><span class="params">(<span class="type">const</span> User&amp; u)</span> </span>&#123;</span><br><span class="line">    db_.<span class="built_in">updateUser</span>(u);                                   <span class="comment">// synchronous</span></span><br><span class="line">    cache_.<span class="built_in">set</span>(<span class="built_in">userKey</span>(u.id), <span class="built_in">serialize</span>(u), kDefaultTtl);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Consistency is strong. But every write pays for both trips. And you fill the board with things that might never be read — the same objection from section 2.</p><p>Often paired with <strong>Read Through</strong>, where the cache handles the cabinet trip on a miss too, so your code only ever talks to the cache.</p><h3 id="Write-Back-Write-Behind"><a href="#Write-Back-Write-Behind" class="headerlink" title="Write Back (Write Behind)"></a>Write Back (Write Behind)</h3><p>Scribble on the board, say “done”, and file it properly later.</p><p>Writes are very fast. Batching helps even more, since ten changes to one entry collapse into one trip. The catch is that you said “done” while the only copy was on a whiteboard. <strong>A crash loses it.</strong> And for a while the cabinet is not actually the truth, which complicates everything that reads it directly.</p><p>This is not exotic. Your OS page cache works this way, so do the store buffers in your CPU. But those have carefully built recovery paths. If you are doing it in application code over Redis, be sure you can afford to lose the window.</p><h3 id="Write-Around"><a href="#Write-Around" class="headerlink" title="Write Around"></a>Write Around</h3><p>Go straight to the cabinet. Do not touch the board at all.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">WriteAroundRepo::insertAuditLog</span><span class="params">(<span class="type">const</span> AuditEntry&amp; e)</span> </span>&#123;</span><br><span class="line">    db_.<span class="built_in">insertAuditLog</span>(e);   <span class="comment">// no cache involved</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Right for things written constantly and read almost never — audit logs, events, telemetry. Putting them on the board would push out things people actually read. This only works if the key is not on the board already. If it might be, you still need the wipe.</p><table><thead><tr><th>Pattern</th><th>Write cost</th><th>Consistency</th><th>Good for</th></tr></thead><tbody><tr><td>Cache Aside</td><td>DB write + erase</td><td>Eventual, small window</td><td>Most things, read-heavy</td></tr><tr><td>Write Through</td><td>DB + cache, in series</td><td>Strong</td><td>Read-right-after-write on hot keys</td></tr><tr><td>Write Back</td><td>Cache only</td><td>Weak, can lose data</td><td>Extreme write throughput</td></tr><tr><td>Write Around</td><td>DB only</td><td>N&#x2F;A</td><td>Write-heavy, read-seldom</td></tr></tbody></table><h2 id="7-Three-problems-the-diagrams-leave-out"><a href="#7-Three-problems-the-diagrams-leave-out" class="headerlink" title="7. Three problems the diagrams leave out"></a>7. Three problems the diagrams leave out</h2><p>Consistency is the famous cache problem. These three take down more systems.</p><h3 id="Penetration-—-asking-for-something-that-does-not-exist"><a href="#Penetration-—-asking-for-something-that-does-not-exist" class="headerlink" title="Penetration — asking for something that does not exist"></a>Penetration — asking for something that does not exist</h3><p>Someone asks for <code>user:999999999</code>. It is not on the board. So you walk to the cabinet. It is not there either. There is nothing to write down, so the board stays blank — and the next identical request walks to the cabinet again.</p><p>Point an attacker at that and your cache stops existing.</p><p>The fix is to write down the fact that it is missing.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">constexpr</span> std::string_view kTombstone = <span class="string">&quot;__NULL__&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function">std::optional&lt;User&gt; <span class="title">UserRepo::getUser</span><span class="params">(<span class="type">int64_t</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="keyword">auto</span> key = <span class="built_in">userKey</span>(id);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">auto</span> cached = cache_.<span class="built_in">get</span>(key)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (*cached == kTombstone) <span class="keyword">return</span> std::<span class="literal">nullopt</span>;   <span class="comment">// known to be absent</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">deserialize</span>&lt;User&gt;(*cached);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> user = db_.<span class="built_in">selectUser</span>(id);</span><br><span class="line">    <span class="keyword">if</span> (!user) &#123;</span><br><span class="line">        cache_.<span class="built_in">set</span>(key, kTombstone, std::chrono::<span class="built_in">minutes</span>(<span class="number">1</span>));  <span class="comment">// short TTL</span></span><br><span class="line">        <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cache_.<span class="built_in">set</span>(key, <span class="built_in">serialize</span>(*user), kDefaultTtl);</span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Keep that TTL short, so a row created later shows up quickly. If the space of possible-but-missing keys is huge, put a Bloom filter in front instead.</p><h3 id="Breakdown-—-one-popular-entry-expires"><a href="#Breakdown-—-one-popular-entry-expires" class="headerlink" title="Breakdown — one popular entry expires"></a>Breakdown — one popular entry expires</h3><p>An entry that thousands of requests depend on expires. All of them miss at the same instant. All of them walk to the cabinet. All of them ask the same question.</p><p>This is the thundering herd. It can take a database down in seconds.</p><p>The fix is to send one person. Everyone else waits for them to come back.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">SingleFlight</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">std::string <span class="title">doOnce</span><span class="params">(<span class="type">const</span> std::string&amp; key,</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="type">const</span> std::function&lt;std::string()&gt;&amp; loader)</span> </span>&#123;</span><br><span class="line">        std::shared_future&lt;std::string&gt; fut;</span><br><span class="line">        std::promise&lt;std::string&gt; prom;</span><br><span class="line">        <span class="type">bool</span> leader = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mu_)</span></span>;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">auto</span> it = inflight_.<span class="built_in">find</span>(key); it != inflight_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">                fut = it-&gt;second;                  <span class="comment">// someone already went</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                fut = prom.<span class="built_in">get_future</span>().<span class="built_in">share</span>();</span><br><span class="line">                inflight_.<span class="built_in">emplace</span>(key, fut);</span><br><span class="line">                leader = <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!leader) <span class="keyword">return</span> fut.<span class="built_in">get</span>();             <span class="comment">// wait for them</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            prom.<span class="built_in">set_value</span>(<span class="built_in">loader</span>());</span><br><span class="line">        &#125; <span class="built_in">catch</span> (...) &#123;</span><br><span class="line">            prom.<span class="built_in">set_exception</span>(std::<span class="built_in">current_exception</span>());</span><br><span class="line">        &#125;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard <span class="title">lock</span><span class="params">(mu_)</span></span>;</span><br><span class="line">            inflight_.<span class="built_in">erase</span>(key);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> fut.<span class="built_in">get</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::mutex mu_;</span><br><span class="line">    std::unordered_map&lt;std::string, std::shared_future&lt;std::string&gt;&gt; inflight_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>This collapses the herd <em>within one process</em>. With twenty instances you still get twenty queries instead of twenty thousand. Usually that is enough. If it is not, use a lock in Redis, or store an expiry time inside the value and have one request refresh it in the background while everyone else keeps serving the slightly old copy.</p><h3 id="Avalanche-—-the-whole-board-gets-wiped-at-once"><a href="#Avalanche-—-the-whole-board-gets-wiped-at-once" class="headerlink" title="Avalanche — the whole board gets wiped at once"></a>Avalanche — the whole board gets wiped at once</h3><p>You warm the cache in a loop at deploy time. Every entry gets the same TTL. Thirty minutes later, every entry expires in the same second, and all of your traffic walks to the cabinet together.</p><p>Or Redis just falls over, and 100% of traffic hits the database at once.</p><p>For the first one, add jitter:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">std::<span class="function">chrono::seconds <span class="title">jitteredTtl</span><span class="params">(std::chrono::seconds base)</span> </span>&#123;</span><br><span class="line">    <span class="type">static</span> <span class="keyword">thread_local</span> std::mt19937 rng&#123;std::random_device&#123;&#125;()&#125;;</span><br><span class="line">    <span class="function">std::uniform_int_distribution&lt;<span class="type">int</span>&gt; <span class="title">jitter</span><span class="params">(<span class="number">0</span>, base.count() / <span class="number">10</span>)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> base + std::chrono::<span class="built_in">seconds</span>(<span class="built_in">jitter</span>(rng));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Ten percent of spread is enough to smear the expiries across minutes instead of piling them into one second. It costs nothing. Make it your default, not something you add after the incident.</p><p>The second one needs infrastructure. Run Redis replicated or clustered, and put a circuit breaker in front of the database so that when the cache disappears your app sheds load instead of forwarding all of it to something that is about to fall over too.</p><h2 id="Wrapping-up"><a href="#Wrapping-up" class="headerlink" title="Wrapping up"></a>Wrapping up</h2><p>The rules that survive contact with production:</p><ol><li><strong>The cabinet is the truth, the board is a convenience.</strong> When in doubt, wipe the board.</li><li><strong>File first, then wipe.</strong> Erase, do not rewrite — erasing is idempotent and order stops mattering. Do it after the commit, never inside the transaction.</li><li><strong>Always set a TTL.</strong> It is the ceiling on every mistake you have not found yet.</li><li><strong>Jitter it.</strong> One line. Prevents a whole class of outage.</li><li><strong>Double delete narrows the window. The logbook closes it.</strong> Know the difference, and do not let 500ms stand in for a guarantee.</li><li><strong>Consistency is not the scariest failure.</strong> Penetration, herds and avalanches will hurt you first.</li></ol><p>The whole thing comes down to one fact: a cache is a second copy of your data. Every problem above follows from that. You cannot make two copies agree perfectly without giving up the speed that made you want the second copy in the first place.</p><p>What you <em>can</em> do is decide how wrong they are allowed to get, and for how long.</p><p>Pick the weakest consistency your product can actually live with. Then make the failures cheap.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="https://redis.io/docs/latest/develop/reference/eviction/">Redis: Key eviction and caching patterns</a></li><li><a href="https://docs.aws.amazon.com/AmazonElastiCache/latest/dg/Strategies.html">AWS: Caching strategies</a> — lazy loading, write through, TTL guidance</li><li><a href="https://debezium.io/documentation/">Debezium documentation</a> — CDC for MySQL, Postgres and others</li><li><a href="https://github.com/alibaba/canal">Canal</a> — Alibaba’s MySQL binlog subscription tool</li><li><a href="https://dev.mysql.com/doc/refman/8.0/en/binary-log.html">MySQL: The binary log</a> — why <code>ROW</code> format is required</li><li><a href="https://pkg.go.dev/golang.org/x/sync/singleflight">groupcache singleflight</a> — the canonical request-collapsing implementation</li><li><a href="https://www.usenix.org/system/files/conference/nsdi13/nsdi13-final170_update.pdf">Scaling Memcache at Facebook</a> — leases, herd control and invalidation at scale, NSDI ‘13</li></ul>]]></content>
      
      
      <categories>
          
          <category> Back to basics </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Back to basics </tag>
            
            <tag> System Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Do you really master LRU?</title>
      <link href="/2026/07/09/different-lru/"/>
      <url>/2026/07/09/different-lru/</url>
      
        <content type="html"><![CDATA[<blockquote><p>Every cache is a bet that you’ll want the same thing twice. Since caches are finite, every cache also needs an answer to an awkward question: when it’s full and something new arrives, what gets thrown out?</p><p>LRU is the most common answer. It’s also the answer that almost no production system implements literally. That gap is the interesting part.</p></blockquote><h2 id="1-What-is-LRU"><a href="#1-What-is-LRU" class="headerlink" title="1. What is LRU?"></a>1. What is LRU?</h2><p>LRU stands for <strong>Least Recently Used</strong>. When the cache is full, evict whatever hasn’t been touched for the longest time.</p><p>The policy rests on one assumption: <strong>temporal locality</strong>. If something was accessed recently, it’ll probably be accessed again soon. This holds remarkably well for a lot of real workloads: web sessions, database pages, file blocks, CPU memory access.</p><p>It’s worth seeing where LRU sits among the alternatives:</p><table><thead><tr><th>Policy</th><th>Evicts</th><th>Trade-off</th></tr></thead><tbody><tr><td><strong>FIFO</strong></td><td>Oldest inserted</td><td>Trivially cheap. Ignores usage entirely.</td></tr><tr><td><strong>LRU</strong></td><td>Oldest accessed</td><td>Good default. Costs bookkeeping on every read.</td></tr><tr><td><strong>LFU</strong></td><td>Least frequently accessed</td><td>Better for stable hot sets. Slow to forget old favourites.</td></tr><tr><td><strong>Random</strong></td><td>Anything</td><td>Nearly free, surprisingly hard to beat by much.</td></tr><tr><td><strong>Belady’s OPT</strong></td><td>The item used furthest in the future</td><td>Optimal, and impossible: it requires clairvoyance. Used as a benchmark.</td></tr></tbody></table><p>LRU wins as a default because it’s the cheapest policy that actually pays attention to what your program is doing.</p><h2 id="2-Put-it-simply"><a href="#2-Put-it-simply" class="headerlink" title="2. Put it simply"></a>2. Put it simply</h2><p>Imagine your desk has room for exactly five sheets of paper.</p><p>You work through them all day. Each time you need a sheet, you pull it out, read it, and put it back <strong>on top of the pile</strong>. Sheets you use constantly keep floating to the top. Sheets you haven’t touched since morning sink toward the bottom.</p><p>Now a sixth sheet arrives and there’s no room. Which one do you get rid of? The one at the bottom, the one you haven’t touched the longest.</p><p>That’s LRU. The whole policy.</p><p>The subtlety is in <em>how you maintain the pile</em>. If you just dumped all five sheets in a heap and wrote the time on each one, finding the oldest would mean checking all five every single time. Fine for five sheets. Terrible for five million.</p><p>The trick that makes LRU practical is that <strong>you keep the pile sorted as you go</strong>, incrementally. Every access does one tiny bit of reordering work, so eviction is instant: just grab the bottom.</p><p>Which leads to the thing that makes LRU expensive in ways people don’t expect: <strong>every read is also a write</strong>. Reading from an LRU cache mutates it, because reading changes the order. Hold that thought. It’s why section 4 exists.</p><h2 id="3-Implementations"><a href="#3-Implementations" class="headerlink" title="3. Implementations"></a>3. Implementations</h2><h3 id="The-naive-version"><a href="#The-naive-version" class="headerlink" title="The naive version"></a>The naive version</h3><p>Store a timestamp with each entry. To evict, scan everything and drop the oldest.</p><p>Reads are O(1), evictions are O(n). For a small cache this is genuinely fine, and its simplicity has real value. For anything large it falls over.</p><h3 id="The-classic-hash-map-doubly-linked-list"><a href="#The-classic-hash-map-doubly-linked-list" class="headerlink" title="The classic: hash map + doubly linked list"></a>The classic: hash map + doubly linked list</h3><p>This is the canonical O(1) solution, and the one interviewers are fishing for.</p><p>Two structures working together:</p><ul><li>A <strong>doubly linked list</strong> holds entries in access order. Most recent at the head, least recent at the tail.</li><li>A <strong>hash map</strong> maps each key directly to its node in that list.</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">head (most recent)                          tail (least recent)</span><br><span class="line">  ↓                                                  ↓</span><br><span class="line">[ D ] ⇄ [ A ] ⇄ [ C ] ⇄ [ B ]         evict from here ──►</span><br><span class="line"></span><br><span class="line">hash map: &#123; A: •, B: •, C: •, D: • &#125;  ──► points into the list</span><br></pre></td></tr></table></figure><p>The hash map gives you O(1) lookup. The linked list gives you O(1) reordering. Neither works alone:</p><ul><li>A list alone means scanning to find a key: O(n).</li><li>A hash map alone has no notion of order.</li></ul><p>The list must be <strong>doubly</strong> linked. To unlink a node in O(1) you need its predecessor, and only a backward pointer gives you that for free.</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span>:</span><br><span class="line">    __slots__ = (<span class="string">&quot;key&quot;</span>, <span class="string">&quot;value&quot;</span>, <span class="string">&quot;prev&quot;</span>, <span class="string">&quot;next&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, key=<span class="literal">None</span>, value=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.key, <span class="variable language_">self</span>.value = key, value</span><br><span class="line">        <span class="variable language_">self</span>.prev = <span class="variable language_">self</span>.<span class="built_in">next</span> = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LRUCache</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, capacity: <span class="built_in">int</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.capacity = capacity</span><br><span class="line">        <span class="variable language_">self</span>.<span class="built_in">map</span>: <span class="built_in">dict</span>[<span class="built_in">int</span>, Node] = &#123;&#125;</span><br><span class="line">        <span class="comment"># Sentinel nodes remove every edge case around empty/single-element lists.</span></span><br><span class="line">        <span class="variable language_">self</span>.head, <span class="variable language_">self</span>.tail = Node(), Node()</span><br><span class="line">        <span class="variable language_">self</span>.head.<span class="built_in">next</span> = <span class="variable language_">self</span>.tail</span><br><span class="line">        <span class="variable language_">self</span>.tail.prev = <span class="variable language_">self</span>.head</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_unlink</span>(<span class="params">self, node: Node</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        node.prev.<span class="built_in">next</span> = node.<span class="built_in">next</span></span><br><span class="line">        node.<span class="built_in">next</span>.prev = node.prev</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_push_front</span>(<span class="params">self, node: Node</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        node.<span class="built_in">next</span> = <span class="variable language_">self</span>.head.<span class="built_in">next</span></span><br><span class="line">        node.prev = <span class="variable language_">self</span>.head</span><br><span class="line">        <span class="variable language_">self</span>.head.<span class="built_in">next</span>.prev = node</span><br><span class="line">        <span class="variable language_">self</span>.head.<span class="built_in">next</span> = node</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self, key: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        node = <span class="variable language_">self</span>.<span class="built_in">map</span>.get(key)</span><br><span class="line">        <span class="keyword">if</span> node <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span></span><br><span class="line">        <span class="variable language_">self</span>._unlink(node)       <span class="comment"># a read mutates the structure</span></span><br><span class="line">        <span class="variable language_">self</span>._push_front(node)</span><br><span class="line">        <span class="keyword">return</span> node.value</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">put</span>(<span class="params">self, key: <span class="built_in">int</span>, value: <span class="built_in">int</span></span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        node = <span class="variable language_">self</span>.<span class="built_in">map</span>.get(key)</span><br><span class="line">        <span class="keyword">if</span> node <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            node.value = value</span><br><span class="line">            <span class="variable language_">self</span>._unlink(node)</span><br><span class="line">            <span class="variable language_">self</span>._push_front(node)</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">len</span>(<span class="variable language_">self</span>.<span class="built_in">map</span>) &gt;= <span class="variable language_">self</span>.capacity:</span><br><span class="line">            lru = <span class="variable language_">self</span>.tail.prev</span><br><span class="line">            <span class="variable language_">self</span>._unlink(lru)</span><br><span class="line">            <span class="keyword">del</span> <span class="variable language_">self</span>.<span class="built_in">map</span>[lru.key]</span><br><span class="line"></span><br><span class="line">        node = Node(key, value)</span><br><span class="line">        <span class="variable language_">self</span>.<span class="built_in">map</span>[key] = node</span><br><span class="line">        <span class="variable language_">self</span>._push_front(node)</span><br></pre></td></tr></table></figure><p>Both <code>get</code> and <code>put</code> are O(1). Memory cost is the payload plus two pointers per entry: 16 bytes on a 64-bit machine, which sounds trivial until you’re caching a hundred million small keys.</p><p>The sentinel head and tail nodes are worth noting. Without them, every unlink and insert needs null checks for “is this the only node,” “is this the first node,” and so on. With them, the list is never actually empty and the code has no branches.</p><h3 id="The-three-line-version"><a href="#The-three-line-version" class="headerlink" title="The three-line version"></a>The three-line version</h3><p>Most languages ship this already. Java’s <code>LinkedHashMap</code> has an access-order mode built in:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">LinkedHashMap</span>&lt;K, V&gt;(capacity, <span class="number">0.75f</span>, <span class="literal">true</span>) &#123;  <span class="comment">// true = access order</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="type">boolean</span> <span class="title function_">removeEldestEntry</span><span class="params">(Map.Entry&lt;K, V&gt; eldest)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> size() &gt; capacity;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>Python’s <code>functools.lru_cache</code> decorator does it for function memoisation, and <code>collections.OrderedDict</code> exposes <code>move_to_end()</code> if you want to build it yourself. Go has <code>container/list</code>, and the community standard is <code>hashicorp/golang-lru</code>. C++ pairs <code>std::list</code> with <code>std::unordered_map</code>.</p><p>Reach for these first. Write the manual version when you need to customise eviction, or when someone’s asking you to on a whiteboard.</p><h3 id="Where-the-textbook-version-breaks-concurrency"><a href="#Where-the-textbook-version-breaks-concurrency" class="headerlink" title="Where the textbook version breaks: concurrency"></a>Where the textbook version breaks: concurrency</h3><p>Here’s the practical problem. Since every read mutates the linked list, <strong>every read needs a write lock</strong>. A cache is usually read-heavy (that’s the entire point of a cache), so you’ve built a structure whose hot path is a global mutex. It becomes the bottleneck immediately under concurrent load.</p><p>Real systems escape this three ways:</p><ol><li><strong>Shard it.</strong> Split into N independent caches keyed by hash. Contention drops roughly N-fold. Eviction is now per-shard rather than global, so it’s no longer true LRU, but it’s close enough and it’s the simplest fix that works.</li><li><strong>Buffer the accesses.</strong> Record reads into per-thread ring buffers and replay them onto the list later, in batches. Reads become lock-free. This is what Caffeine does.</li><li><strong>Stop tracking exact order.</strong> Approximate it with a bit or a coarse timestamp per entry. This is what almost every large system actually does, which brings us to the next section.</li></ol><h2 id="4-How-real-systems-bend-LRU"><a href="#4-How-real-systems-bend-LRU" class="headerlink" title="4. How real systems bend LRU"></a>4. How real systems bend LRU</h2><p>Textbook LRU has three problems at scale: the memory overhead of the pointers, the lock contention above, and one more that’s easy to miss.</p><p><strong>Scan resistance.</strong> Suppose your cache is holding a nicely warmed hot set, and then some batch job does a single full-table scan. Every page in that scan is “recently used,” so LRU dutifully evicts your entire hot set in favour of data that will never be read again. One nightly report can destroy a cache’s hit rate. Pure LRU has no defence against this at all, because it can’t distinguish “used once, just now” from “used constantly.”</p><p>Here’s how different systems respond.</p><h3 id="Redis-sample-don’t-sort"><a href="#Redis-sample-don’t-sort" class="headerlink" title="Redis: sample, don’t sort"></a>Redis: sample, don’t sort</h3><p>Redis doesn’t maintain a global LRU list. Doing so would cost two pointers per object. For a server holding hundreds of millions of small keys, that overhead is unacceptable.</p><p>Instead, each object header carries a <strong>24-bit clock timestamp</strong>, updated on access. When memory is tight, Redis picks a few random keys (<code>maxmemory-samples</code>, default 5), compares their timestamps, and evicts the oldest of the sample.</p><p>That’s it. It’s LRU by opinion poll. With 5 samples the result is a decent approximation of true LRU; raising the value gets closer at higher CPU cost. Redis 3.0 improved the accuracy by keeping a pool of the best eviction candidates seen so far, carried across rounds, so good candidates aren’t forgotten between samples.</p><p>Redis 4.0 later added <strong>LFU</strong> mode (<code>allkeys-lfu</code>), using an 8-bit logarithmic counter with time-based decay. The motivation is precisely the scan problem: with LRU, a key touched once ranks equal to a key touched ten thousand times.</p><h3 id="Linux-kernel-two-lists-then-many"><a href="#Linux-kernel-two-lists-then-many" class="headerlink" title="Linux kernel: two lists, then many"></a>Linux kernel: two lists, then many</h3><p>The page cache uses a <strong>two-list strategy</strong>: an <em>active</em> list and an <em>inactive</em> list.</p><p>New pages land on the inactive list. They only get promoted to active on a <strong>second</strong> reference. Eviction pressure falls on the inactive list first.</p><p>This is scan resistance, cheaply achieved. A sequential read touches each page once, so those pages cycle through the inactive list and get reclaimed without ever displacing the genuinely hot pages on the active list. Ordering within lists is approximated with a referenced bit (CLOCK-style) rather than tracked exactly.</p><p>Linux 6.1 merged <strong>MGLRU</strong> (Multi-Generational LRU), which generalises two lists into multiple generations. Pages age through generations, and eviction targets the oldest. It reduced reclaim overhead substantially and improved hit rates under memory pressure.</p><h3 id="MySQL-InnoDB-insert-in-the-middle"><a href="#MySQL-InnoDB-insert-in-the-middle" class="headerlink" title="MySQL InnoDB: insert in the middle"></a>MySQL InnoDB: insert in the middle</h3><p>InnoDB’s buffer pool splits its LRU list into a “young” sublist and an “old” sublist, with new pages inserted at the boundary, about <strong>3&#x2F;8 of the way from the tail</strong>, not at the head.</p><p>There’s a second guard: <code>innodb_old_blocks_time</code> (default 1000ms). A page must survive in the old sublist for at least that long <em>and</em> be accessed again before it’s promoted to young.</p><p>Both mechanisms exist for the same reason. A full table scan reads each page once, in quick succession. Under textbook LRU it would flush the buffer pool. Under midpoint insertion, those pages never make it out of the old sublist.</p><h3 id="CPU-caches-pseudo-LRU"><a href="#CPU-caches-pseudo-LRU" class="headerlink" title="CPU caches: pseudo-LRU"></a>CPU caches: pseudo-LRU</h3><p>True LRU in hardware needs enough state to order every line in a set, which is impractical beyond low associativity. Processors use <strong>tree-PLRU</strong> instead, a binary tree of direction bits needing only n−1 bits per set. It points at <em>an</em> old line rather than <em>the</em> oldest. In silicon, close enough at a fraction of the cost is the right trade.</p><h3 id="Caffeine-admission-control"><a href="#Caffeine-admission-control" class="headerlink" title="Caffeine: admission control"></a>Caffeine: admission control</h3><p>The most interesting modern variant. <strong>W-TinyLFU</strong> asks a different question: not “what should I evict?” but “<strong>should I admit this at all?</strong>“</p><p>A compact Count-Min Sketch tracks approximate access frequencies across a much larger key space than the cache itself holds. When a new item arrives and something must be evicted, the candidate is compared against the eviction victim, and the new item is only admitted if it’s <em>more</em> frequent. A small LRU window in front handles items that are genuinely new rather than genuinely unpopular.</p><p>The result is that a scan can’t pollute the cache: scanned items simply never earn admission. Caffeine consistently beats LRU on real-world traces, often substantially.</p><p>Worth also knowing: <strong>ARC</strong> (Adaptive Replacement Cache) balances recency and frequency using “ghost” lists of recently evicted keys, adapting its split automatically. ZFS uses it. PostgreSQL famously backed away from it over patent concerns and uses a clock-sweep algorithm instead.</p><h2 id="5-Wrapping-up"><a href="#5-Wrapping-up" class="headerlink" title="5. Wrapping up"></a>5. Wrapping up</h2><p>LRU is the right default. It’s simple, it matches how programs actually behave, and the hash map plus doubly linked list gives you O(1) on everything.</p><p>But notice what happened every time we looked at a real system. Nobody implements the textbook version, and they all deviate in the same three directions:</p><ul><li><strong>Approximate the ordering.</strong> Redis samples, Linux uses reference bits, CPUs use a bit tree. Exact recency ordering is rarely worth what it costs.</li><li><strong>Segment the cache.</strong> Linux’s active&#x2F;inactive lists, InnoDB’s young&#x2F;old sublists, Caffeine’s window. Segmentation is how you buy scan resistance.</li><li><strong>Filter what gets in.</strong> W-TinyLFU’s admission policy is the sharpest version of this, and the biggest departure from LRU thinking.</li></ul><p>Those three moves are the actual lesson. LRU is one point in a design space defined by memory overhead, lock contention, and how badly a sequential scan can hurt you. Knowing where your workload sits on those axes matters more than knowing the data structure.</p><p>And if you’re preparing for interviews: implement the linked list version until it’s automatic, then be ready to explain why the read path needs a write lock. That second question is where the conversation gets interesting.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="https://redis.io/docs/latest/develop/reference/eviction/">Redis: Key eviction</a>: sampling, the eviction pool, and LFU mode</li><li><a href="http://antirez.com/news/109">Redis LRU implementation notes</a>: antirez on why approximation was the right call</li><li><a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-buffer-pool.html">MySQL: Buffer Pool LRU algorithm</a>: midpoint insertion parameters</li><li><a href="https://docs.kernel.org/admin-guide/mm/multigen_lru.html">Linux MGLRU documentation</a>: the multi-generational design</li><li><a href="https://github.com/ben-manes/caffeine/wiki/Efficiency">Caffeine: Efficiency</a>: W-TinyLFU benchmarks against LRU and ARC</li><li><a href="https://arxiv.org/abs/1512.00727">TinyLFU: A Highly Efficient Cache Admission Policy</a>: Einziger, Friedman &amp; Manes</li><li><a href="https://www.usenix.org/legacy/events/fast03/tech/full_papers/megiddo/megiddo.pdf">ARC: A Self-Tuning, Low Overhead Replacement Cache</a>: Megiddo &amp; Modha, FAST ‘03</li><li><a href="https://leetcode.com/problems/lru-cache/">LeetCode 146: LRU Cache</a>: the standard exercise</li></ul>]]></content>
      
      
      <categories>
          
          <category> Back to basics </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Back to basics </tag>
            
            <tag> System Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Phone Detox Experiment</title>
      <link href="/2026/07/07/dopamine-detox/"/>
      <url>/2026/07/07/dopamine-detox/</url>
      
        <content type="html"><![CDATA[<h1 style="color:#2E86AB">Before the experiment 🧠</h1>Actually, getting lost scrolling through my phone has been a common thing for many of us, especially in a modern world, where we are always connected with others through social media. High tech IT companies hire top talents to design delicated algorithms, striving to stick users to their product, and then make profits. Don't be guilty for that, it's not our fault. That being said, we still ought to view this matter objectively. Internet grant us access to abundant information, yet we should not get trapped by cognitive overload.So, I decided to try a phone detox experiment, help me discover a world that I could talk more with myself.<h1 style="color:#2E86AB">Experiment design</h1><p>As usual, here’s the 7-day plan of “Phone Detox Experiment”. I made this plan with ChatGPT based on my own experience. Hope I will follow the plan strictly🤣.<br><img src="/images/dopamine_detox/image1.png"></p><h1 style="color:#2E86AB">Daily note 🤔</h1><h3 id="Day-1"><a href="#Day-1" class="headerlink" title="Day 1"></a>Day 1</h3><blockquote><p>Target: Awareness - How often I reach for devices?</p></blockquote><p><strong>The day has just begun, I’m hoping to get off to great start💪. We’ll wait and see</strong> </p><ul><li>8:30 – I woke up, and started my day. The first thing I did was to check my phone for new notifications from Gmail and Wechat. Then sitting on the toilet, I scrolled through Reddit threads about new AI workflow and car modification stuff…</li><li>11:30 – Shortly after I wrote down the plan, I felt the urge to scroll TikTok to check if I’d gotten any likes👍🏽. Bloody hell…</li><li>14:00 – Went to the toilet, scolling through RedNote for what’s new around me, like car exhaust clips, new harness usage, and stock market exploding news.</li><li>17:00 – Working out, frequently checked my phone during set break, even there weren’t any new messages. It’s like a built-in background check, yep, this isn’t the thing I expected.</li><li>19:00 – Listening to English podcast when cooking, thinking that I shouldn’t waste any piece of time…</li><li>23:30 – Went to bed, and listened to some audio fiction books to help fell asleep, for 30min…</li></ul><p><em>Wrap up: I felt like I was good enough to keep me away from the phone, but actually I was just get lost depending on information from my phone constantly. Maybe it’s because of information anxiety, which means my inner mind is to to as much information as I could get, otherwise I would fall behind. Hah, typical Chinese guy🐼.</em></p><div align="center">    <img src="/images/dopamine_detox/image2.jpeg" width="50%" /></div><h3 id="Day-2"><a href="#Day-2" class="headerlink" title="Day 2"></a>Day 2</h3><blockquote><p>Target: Embrace of BOREDOM</p></blockquote><p><strong>Today, I wouldn’t use phone during waiting time and toilet time. In my opinion, if I could make it then I am a total “badass🏎️”</strong></p><ul><li><p>8:00 – Got up, going directly to wash and brush teeth. Then cook breakfast without even thinking of the phone, although sometime I could hear a voice from my built-in “bad” system told me to check my phone for something INTERESTING (actuall 99.99% useless content).</p></li><li><p>9:00 – Went to school, and started my day without taking phone with me. In fact, I felt kind of frustrated that I couldn’t get some “happniess” from time to time.</p></li><li><p>9:30 – It feels just like quitting alcohol. Even though, I’ve never gone through it myself, my brain was constantly itching to open all unread messages.</p></li><li><p>12:30 – I did 10min meditation, enduring the “tedious” moment, trying to feel the temperature of my breathe, as well as the noise of the silence. After that, I felt way easier to start working on my school tasks.</p></li><li><p>14:00 – Headed to the toilet WITHOUT phone. I bet this will be the fastest trip yet🤣.</p></li><li><p>17:00 – I work out WITHOUT my phone. Thoughts keep popping into my head during rest periods between sets. I wonder if I can bench press 100 kilograms and how I would celebrate hitting that milestone. After all, I’m a total “badass”.</p></li><li><p>23:00 - I jumped into bed and saw a flood of notifications and messages. Ninety-nine percent of them are just meaningless clutter. This is the honest truth.</p></li></ul><div align="center">    <img src="/images/dopamine_detox/image3.jpg" width="50%" /></div><h3 id="Day-3"><a href="#Day-3" class="headerlink" title="Day 3"></a>Day 3</h3><blockquote><p>Target: Focus on depth</p></blockquote><p><strong>Kick short videos and RedNote clips away!</strong></p><ul><li><p>9:00 – I got up and then took a shower with slightly cold water. Unexpectedly, I felt thankful for my plan and began to feel full of energy. I was far more awake than I had been on previous mornings.</p></li><li><p>10:00 – I read the book Goosebumps, a horror series written for children. It works perfectly for someone like me who does not speak English as a first language. The plot twists are absolutely gripping. Time passed so quickly that two hours slipped away in an instant…</p></li><li><p>14:00 –I left my phone at home. I brought my laptop to the library and tried writing a blog about what I hae been studying recently. The whole afternoon passed, yet I still haven’t finished it…</p></li><li><p>22:30 – It was a peaceful night. I lay on my bed and wondered why I could not use the English phrases I had jotted down and reviewed. Could cognitive overload be the reason?</p></li></ul><h3 id="Day-4"><a href="#Day-4" class="headerlink" title="Day 4"></a>Day 4</h3><blockquote><p>Target: Deep work day</p></blockquote><p><strong>Try doing whiteboard recall to check if I have truly mastered a concept</strong></p><ul><li><p>9:00 – Left the phone at home, started my day.</p></li><li><p>10:00 – Spent all day researching Harness engineering and how we could integrate it into our daily life. It’s so fun. Although I tried to do whiteboard to check my RDMA congestion control protocol knowledge. OK, my bad.</p></li><li><p>22:30 – Jumped to bed, thought about the research I did today. And by the time, I forget where my phone was.</p></li></ul><h3 id="Day-5"><a href="#Day-5" class="headerlink" title="Day 5"></a>Day 5</h3><blockquote><p>Target: Real life activities</p></blockquote><p><strong>Take part in a volunteer role at Auckland Food Show</strong></p><ul><li><p>8:00 – Got up early, then jogged for 30min at Mission Bay. That great, even though it was cloudy in the morning.</p></li><li><p>10:00 – Did some household chores, and I was not urgent to touch the phone or trying to check messages.</p></li><li><p>12:30 – Busy volunteering at the Auckland Food Show, I spoke with attendees and shared the mission of Auckland Mission Grocer with them. Many generous people donated our food packages to those in need. This experience gave me a strong sense of community, and I felt proud to take on this volunteer work.</p></li><li><p>22:30 – Such a busy day, I had not even 1 minute to scroll through video clips. It was sufficient and fulfilling.</p></li></ul><div align="center">    <img src="/images/dopamine_detox/image4.jpeg" width="50%" /></div><h3 id="Day-6"><a href="#Day-6" class="headerlink" title="Day 6"></a>Day 6</h3><blockquote><p>Target: Connect with people</p></blockquote><p><strong>Make new friends in the class!</strong></p><ul><li><p>10:00 – I didn’t get up very early because I couldn’t fall asleep last night. Lots of thoughts kept popping into my head.</p></li><li><p>13:00 – Two stutents were looking for a position in the class, I greeted them and told them to sit down beside me. I started a conversation with them, and we talked about the class and we formed a group to make a presentation. They are from Nepal and Indian respectively. Cuz my listening is not as good as a native speaker, sometimes I struggled to understand them, but finally I nailed by some “Pardon?”. Really, I found the best way of learning is taking action. I became more confident in English, especially when talking with non-native Enlish speaking friends.</p></li><li><p>17:00 – After some time, I invited them to go fishing this weekend. Yep, more talk, less phone.</p></li></ul><h3 id="Day-7"><a href="#Day-7" class="headerlink" title="Day 7"></a>Day 7</h3><blockquote><p>Target: Reflection</p></blockquote><p><strong>What have I learned from this experiment?</strong></p><p>I’d say, I’ve never tried something like this before. I think recording my daily life is a good idea, it doesn’t only help me keep track of what I did and think, but also it’s like a supervisor to monitor and encourage me to continue. It’s much better than just say I would do a “cutting down on phone” experiment.<br>That said, I don’t totally achieve my goal to get rid of the bad habit, but things are getting better 🤗</p><h1 style="color:#2E86AB">What did I get? 🍎</h1><ol><li><strong>What improved?</strong><ul><li>Feel more self-awareness, sometimes I feel more awake and less anxious.</li><li>Extended my concentration span, I can continue doing something I rejected to do previously, such as playing “Don’t Starve Together” for 3 hours🤖.</li><li>Be more patient to people and things around me.</li><li>It’s easier for me to fall asleep. I probably feel more fulfilled, and I finish more tasks each day than I did before.</li></ul></li><li><strong>What was hard?</strong><ul><li>Stop using phone in the toilet</li><li>Prohibit the phone usage in the bedroom before going to bed, it’s a habit for many years after all</li><li>Find a way to stay connected with people in person, I found I am kind of introverted. This is critically important, I must take action to change.</li></ul></li><li><strong>What did I enjoy?</strong><ul><li>Every night I thought I made it~</li><li>Can think through something that I was not interested in.</li><li>The experiment was going smoothly, which means it’s well-designed for me.</li></ul></li><li><strong>What will I continue?</strong><ul><li>Insist on carrying out the experiment, build this good habit, and ask my wife to join in.</li><li>Continue to focus on depth, to feel what’s around me, to enjoy whatever I am doing.</li><li>Be grateful for every moment, every opportunity, every person in my life.</li></ul></li></ol>]]></content>
      
      
      <categories>
          
          <category> Everyday Life </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Everyday life </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Caffeine Detox Experiment</title>
      <link href="/2026/06/30/caffine-detox-exp/"/>
      <url>/2026/06/30/caffine-detox-exp/</url>
      
        <content type="html"><![CDATA[<h1 style="color:#2E86AB">Before the experiment 🧠</h1>When it comes to having coffee, I would think of my experience in TikTok. When it was during the COVID-19 pandemic, I felt so depressed that I tried various ways to help me felt better because of getting quarantined at home. I got a cup of hand brewed pour-over coffee one day, and it was so fabulous. That's how my coffee dependency kicked off. Coffee did bring lots of happy experience to me. That said, coffee has also brought me troubles. For instance, I usually force myself to be awake through iced latte after burning the mid-night oil. I feel I am "awake", but actuall my brain has already broken down. How do I know that? Cuz I was like a "zombie", easy to forget things, hard to retrieve even simple info from memory.I'd say drinking coffee in a correct way would greatly improve life quality. I decided to try a caffeine detox experiment though. Just want to know if it helps.<h1 style="color:#2E86AB">Experiment design</h1><blockquote><p>Owning 0 experience about this field, I do some research online. And here’s a plan, I made it clearer by ChatGPT visualization.</p></blockquote><p>Here’s the 7-day plan of “Caffeine Detox Experiment”, I’d append my daily report below in next 7 days.<br><img src="/images/caffeine_detox/image1.png"></p><h1 style="color:#2E86AB">Daily note 🤔</h1><h3 id="Day-1"><a href="#Day-1" class="headerlink" title="Day 1"></a>Day 1</h3><blockquote><p>Target: Cut down – Reduce usual intake</p></blockquote><p><strong>Felt kind of happy and unhappy today. Feeling happy is because I’m making things different and trying something new to help me with my health. Feeling unhappy as I will lose some of my 🙃dopamine source.</strong></p><p>Maybe this is the last cup of coffee I will drink this week. Tomorrow, I will try with tea or a little bit instant coffee.<br>By the way, simple coffee bean here is so simple in Auckland, at least in most of cafe. Maybe I didn’t get the correct way to be a coffee enthusiast here. I used to having special coffee beans in BeiJing, sometimes I have to admit that there are some benefits for “workforce advantage”🤣.</p><div align="center">    <img src="/images/caffeine_detox/image2.jpg" width="50%" /></div><h3 id="Day-2"><a href="#Day-2" class="headerlink" title="Day 2"></a>Day 2</h3><blockquote><p>Target: Limit to half</p></blockquote><p><strong>Got up early to go boat fishing, and drank a canned coffee to woke me up. Nothing chanllenging, nothing special. Just got seasick 🫠</strong></p><div align="center">    <img src="/images/caffeine_detox/image3.jpg" width="42%" />    <img src="/images/caffeine_detox/image4.jpg" width="42%" /></div><h3 id="Day-3"><a href="#Day-3" class="headerlink" title="Day 3"></a>Day 3</h3><blockquote><p>Target: No caffeine</p></blockquote><p><strong>Do sleep well last night, I got up at near 8am today.Still half-asleep, I made myself a cup of coffee. Then pour it down the sink… Anyway, I felt a little bit dizzy and headache, but it’s okay.</strong></p><div align="center">    <img src="/images/caffeine_detox/image5.jpg" width="50%" alt="Let it go"></div><h3 id="Day-4"><a href="#Day-4" class="headerlink" title="Day 4"></a>Day 4</h3><blockquote><p>Target: Experience the toughest day</p></blockquote><p><strong>Realllllllly tough, even I had a good sleep last night, but during the day I felt a little bit dizzy and headache. Moveover, I couldn’t focus on anything. After 2 hours of struggle, I finally went into the flow.</strong></p><p><strong>Meanwhile, I also found that it was vulnerable to get scrolling throught the screen when I couldn’t gather my mind. Just for reminder, keep phone away from me.🤛</strong></p><p><strong>Knocked out a few sets of back exercises, my head felt much better after that💪🏽</strong></p><h3 id="Day-5"><a href="#Day-5" class="headerlink" title="Day 5"></a>Day 5</h3><blockquote><p>Target: Energy improves</p></blockquote><p><strong>Easily went to sleep last night, get up early today. I go for a walk outside and love soaking up the sun☀️. Even no other coffee, I can also be engergized.</strong></p><div align="center">    <img src="/images/caffeine_detox/image6.jpg" width="50%" alt="So blue, no filter required"></div><h3 id="Day-6"><a href="#Day-6" class="headerlink" title="Day 6"></a>Day 6</h3><blockquote><p>Target: Reach a new balance</p></blockquote><p><strong>I kind of feel nice today, without thinking about coffee or something that gives me dopamine. It’s like I were a flat person, being easy to satisfy and come into a state of flow.</strong><br><strong>Free of distracting thoughts, the mind flows like a gentle, murmuring stream.</strong></p><div align="center">    <img src="/images/caffeine_detox/image7.jpg" width="50%" alt="Wife-made burger"></div><h2 id="Day-7"><a href="#Day-7" class="headerlink" title="Day 7"></a>Day 7</h2><blockquote><p>Target: Natural energy coming back! </p></blockquote><p><strong>Beauuuuuuutiful day! Got up naturally even without alarm clock~ Full of energy all the day and have a car meetup with my friend.</strong><br><strong>Less “cache miss”, more “cache hit” when retrieving information from memory.</strong></p><div align="center">    <img src="/images/caffeine_detox/image8.jpg" width="50%" alt="Labour brings me joy"></div><h1 style="color:#2E86AB">Conclusion🧑🏿‍💻</h1>For me personally, I think the experiment works well. I felt much better after the experiment. I can also focus on my work more easily.That said, I won't give up drinking coffee or something with caffeine thoroughly, instead I would try to reduce the intake. After all, special coffee is fabulous.]]></content>
      
      
      <categories>
          
          <category> Everyday Life </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Everyday life </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Reflection - System Design Process</title>
      <link href="/2026/04/20/system-design-process/"/>
      <url>/2026/04/20/system-design-process/</url>
      
        <content type="html"><![CDATA[<blockquote><p>For a long time I sat on the other side of the table. I interviewed a lot of candidates on system design, and the pattern was consistent: the ones who struggled had no frame to hang the problem on. They’d dive straight into databases or microservices and then get lost. The ones who did well slowed down and worked through the problem in an order that made sense.</p><p>I ended up on the interviewee side recently, and I wanted a process I could actually follow. The infographic is what I landed on.</p></blockquote><p>Six phases, roughly 10 minutes each, totalling about an hour.</p><p><img src="/images/system_design_process.png" alt="Whole picture"></p><h2 id="👔Step-1-business-use-cases-10-min"><a href="#👔Step-1-business-use-cases-10-min" class="headerlink" title="👔Step 1: business use cases (10 min)"></a>👔Step 1: business use cases (10 min)</h2><p>Before any technical decision, understand the problem.</p><p>Who are the users? What are they actually trying to do? What does “working” look like?</p><p>This is the easiest step to skip and the hardest to recover from. If you get the use cases wrong, you’ll end up with something architecturally coherent that solves the wrong problem. Interviewers notice when candidates jump straight to components. The ones who do well ask questions first.</p><h2 id="🦾Step-2-constraints-10-min"><a href="#🦾Step-2-constraints-10-min" class="headerlink" title="🦾Step 2: constraints (10 min)"></a>🦾Step 2: constraints (10 min)</h2><p>Once you know what, figure out how much.</p><p>What’s the expected QPS? What latency does the use case actually need? How much data? What’s the availability target?</p><p>Don’t skip budget, timeline, or compliance. Real systems have real limits. Stating constraints explicitly gives you something to point back to when you make trade-offs later.</p><h2 id="🏰Step-3-architecture-10-min"><a href="#🏰Step-3-architecture-10-min" class="headerlink" title="🏰Step 3: architecture (10 min)"></a>🏰Step 3: architecture (10 min)</h2><p>Now draw the high-level picture.</p><p>What are the major components? Where are the boundaries? What technology are you choosing, and why? Walk through the data flow.</p><p>This doesn’t need to be a final production design. It needs to be a coherent skeleton that can hold up to questions. Show that you can decompose a complex system into parts someone else can follow.</p><h2 id="⌨️Step-4-API-design-10-min"><a href="#⌨️Step-4-API-design-10-min" class="headerlink" title="⌨️Step 4: API design (10 min)"></a>⌨️Step 4: API design (10 min)</h2><p>With the architecture sketched, define the contracts between components.</p><p>What are the key endpoints? What do requests and responses look like? How do you handle auth, errors, versioning?</p><p>Candidates often get vague here. Interviewers who’ve worked in production know that badly designed APIs cause pain for years. Be specific.</p><h2 id="📊Step-5-database-choice-10-min"><a href="#📊Step-5-database-choice-10-min" class="headerlink" title="📊Step 5: database choice (10 min)"></a>📊Step 5: database choice (10 min)</h2><p>Pick the right storage layer, and explain why.</p><p>SQL or NoSQL depends on your data model, access patterns, and consistency requirements. Talk about sharding, indexing, caching, failure handling.</p><p>The point isn’t to always reach for the same tool. It’s to show you can reason about trade-offs: read-heavy vs write-heavy, strong vs eventual consistency. Rattling off database names without that reasoning doesn’t go far.</p><h2 id="📀Step-6-data-model-10-min"><a href="#📀Step-6-data-model-10-min" class="headerlink" title="📀Step 6: data model (10 min)"></a>📀Step 6: data model (10 min)</h2><p>Finally, get concrete about the data.</p><p>Define your entities and their relationships. Specify primary keys and indexes. Think about what happens to the schema at scale.</p><p>A well-thought-out data model often exposes assumptions from earlier steps that don’t hold up. That’s a good sign. Going back and revising shows you’re thinking through the problem, not filling out a template.</p><p><img src="/images/system_design_process_2.png"></p><h2 id="Wrapping-up"><a href="#Wrapping-up" class="headerlink" title="Wrapping up"></a>Wrapping up</h2><blockquote><p>Keep it simple, communicate clearly, iterate and refine.</p></blockquote><p>That’s mostly it. The interview isn’t asking whether you know every database or can sketch the perfect architecture in 10 minutes. It’s asking whether you can navigate an open-ended problem without losing the thread.</p><p>The six steps are a scaffold, not a script. Practice it enough that it becomes automatic. Then the mental energy you’re not spending on process can go toward the parts of the problem that are actually hard.</p>]]></content>
      
      
      <categories>
          
          <category> System Design </category>
          
      </categories>
      
      
        <tags>
            
            <tag> System Design </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>The Silent Performance Killer - False Sharing in C++</title>
      <link href="/2026/03/02/false-sharing/"/>
      <url>/2026/03/02/false-sharing/</url>
      
        <content type="html"><![CDATA[<blockquote><p>One day I ran into a weird performance issue when tunning the ring buffer module of a high performance C++ RPC framework.</p><p>I cooperated with another colleague, aiming to write a fast ring buffer, somehow he updated the key <code>struct</code> definition, we weren’t on the same page though.<br>Eventually the performance benchmark ended up performing poorly.</p><p>In hindsight, the degradation was not <code>directly</code> a bug in the code, but a feature of the hardware. In our benchmark, the throughput under 2 threads was better than that of 8 threads, which was really interesting.</p></blockquote><p>That was caused by <code>false sharing</code>, one of the most counterintuitive performance bugs in concurrent C++.</p><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a><em>TL;DR</em></h2><ul><li><code>False sharing</code> occurs when multiple threads access and modify variables that reside on the same cache line, leading to a degradation in performance.</li><li><code>False sharing</code> can be avoided by using padding or alignment to separate variables on different cache lines.</li></ul><h2 id="What’s-False-Sharing"><a href="#What’s-False-Sharing" class="headerlink" title="What’s False Sharing?"></a>What’s False Sharing?</h2><p>False sharing happens when threads modify variables that happen to live on the same cache line. The variables themselves are unrelated, but the cache line keeps bouncing between cores, invalidated over and over, and that’s what kills the performance.</p><h2 id="Feynman-Method"><a href="#Feynman-Method" class="headerlink" title="Feynman Method"></a>Feynman Method</h2><p>  <img src="/images/false_sharing_7.png"><br>Imagine you and a colleague are sharing a single whiteboard, but you each only need one small corner of it.</p><p>You’re writing your notes in the top-left corner. Your colleague is writing theirs in the top-right corner. You never touch each other’s work. Completely independent.</p><p><strong>But here’s the catch: the whiteboard can only be erased and rewritten one person at a time</strong>. Every time you want to write something, you have to claim the whole whiteboard, which means your colleague has to stop, wait for you to finish, then reclaim it before they can write again. And vice versa.</p><p>Neither of you is doing anything wrong. You’re not interfering with each other’s content at all. But you’re constantly blocking each other from using the board, because the rule operates on the whole whiteboard, not on individual corners.</p><p>That whiteboard is a cache line. Your corner is variable x. Your colleague’s corner is variable y. The “claim the whole board” rule is the <code>MESI</code> coherence protocol.</p><p><strong>The fix is simple: get a second whiteboard. Put your notes on one, your colleague’s notes on the other. Now you never have to wait for each other</strong>. That’s what <code>alignas</code> does, it forces each variable onto its own cache line, its own whiteboard, so the two cores never have to fight over the same one.</p><h2 id="Start-with-an-Example"><a href="#Start-with-an-Example" class="headerlink" title="Start with an Example"></a>Start with an Example</h2><p>Consider the following code snippet:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;new&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">constexpr</span> <span class="type">uint64_t</span> kMaxTimes = <span class="number">1000000000UL</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> 0 </span></span><br><span class="line"><span class="comment">// X : bad example</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FalseSharing</span> &#123;</span><br><span class="line">  <span class="keyword">volatile</span> <span class="type">uint64_t</span> x&#123;<span class="number">0</span>&#125;;</span><br><span class="line">  <span class="keyword">volatile</span> <span class="type">uint64_t</span> y&#123;<span class="number">0</span>&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// √ : good example, fixed the false-sharing issue</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FalseSharing</span> &#123;</span><br><span class="line">  <span class="built_in">alignas</span>(<span class="number">64</span>) <span class="keyword">volatile</span> <span class="type">uint64_t</span> x&#123;<span class="number">0</span>&#125;;</span><br><span class="line">  <span class="built_in">alignas</span>(<span class="number">64</span>) <span class="keyword">volatile</span> <span class="type">uint64_t</span> y&#123;<span class="number">0</span>&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">FalseSharing data;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">WorkerA</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">uint64_t</span> i = <span class="number">0</span>; i &lt; kMaxTimes; ++i) &#123;</span><br><span class="line">    ++data.x;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">WorkerB</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">uint64_t</span> i = <span class="number">0</span>; i &lt; kMaxTimes; ++i) &#123;</span><br><span class="line">    ++data.y;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> start = std::chrono::steady_clock::<span class="built_in">now</span>();</span><br><span class="line"></span><br><span class="line">  <span class="function">std::thread <span class="title">t1</span><span class="params">(WorkerA)</span></span>;</span><br><span class="line">  <span class="function">std::thread <span class="title">t2</span><span class="params">(WorkerB)</span></span>;</span><br><span class="line">  t<span class="number">1.</span><span class="built_in">join</span>();</span><br><span class="line">  t<span class="number">2.</span><span class="built_in">join</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> end = std::chrono::steady_clock::<span class="built_in">now</span>();</span><br><span class="line">  <span class="keyword">auto</span> cost =</span><br><span class="line">      std::chrono::<span class="built_in">duration_cast</span>&lt;std::chrono::milliseconds&gt;(end - start);</span><br><span class="line">  std::cout &lt;&lt; <span class="string">&quot;Time cost totally: &quot;</span> &lt;&lt; cost.<span class="built_in">count</span>() &lt;&lt; <span class="string">&quot; ms \n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Building and running this gives:<br><img src="/images/false_sharing_2.png" alt="Build and Run"></p><ul><li>When false-sharing:<br><img src="/images/false_sharing_3.png" alt="Bad result"></li><li>When fixed:<br><img src="/images/false_sharing_4.png" alt="Good result"></li></ul><p>The performance result gap is significant, it costs <code>1776 ms</code> to run the code snippet with false-sharing, while <code>409 ms</code> to run it with fixed false-sharing issue.<br>And <strong>it will be even worse if we run it on more cores</strong>.</p><p>When reproducing the case by yourself, it should be noted that:</p><ol><li><p>We should compile with <code>-O3</code>, otherwise the compiler will optimize away the false-sharing issue. Sepecifically, it will simply remove the loop and add the final result. It was like “You nerd, why not add the variable all at once??”.</p></li><li><p>We should use <code>volatile</code> to ensure that the compiler doesn’t optimize away the access to the variables <strong>in the example</strong>. Otherwise, the variable is cached in the CPU register, making it hard to observe the performance degradation. Since it optimizes away a ton of <code>DRAM memory access</code>.</p></li></ol><h2 id="Under-the-Hood"><a href="#Under-the-Hood" class="headerlink" title="Under the Hood"></a>Under the Hood</h2><p>CPUs don’t read individual bytes from RAM. They read in fixed-size chunks called cache lines, typically 64 bytes on most modern x86 and ARM processors. When a core accesses any byte in memory, the entire 64-byte line containing it is pulled into L1 cache.</p><p>This is usually a win. But in multithreaded code, it creates a trap.</p><p><img src="/images/false_sharing_1.png"></p><p>Suppose Thread A writes to x and Thread B writes to y. Different variables, different threads, no logical sharing. But if x and y sit within the same 64-byte cache line, the CPU doesn’t know that. Every time Thread A writes, it acquires exclusive ownership of the line, which invalidates Thread B’s copy. Thread B then reloads the line before it can write. Then A’s copy is invalidated. Back and forth, millions of times per second.</p><p>This is false sharing: threads that don’t share data logically, but are forced by hardware to behave as if they do.</p><p>The mechanism is the MESI cache coherence protocol. A write transitions a cache line to Modified on the writing core and broadcasts Invalid to all other cores holding a copy. Each invalidation costs dozens to hundreds of cycles and they never stop.</p><h2 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h2><h3 id="Method1-Padding"><a href="#Method1-Padding" class="headerlink" title="Method1: Padding"></a>Method1: Padding</h3><p>By adding padding to the struct, we can separate the variables onto different cache lines, which avoids false sharing. In C++, we can use keyword <code>alignas</code> to align the variables.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// √ : good example, fixed the false-sharing issue</span></span><br><span class="line"><span class="comment">// Usually 64 byte cache line, in C++17 `alignas(std::hardware_destructive_interference_size)` is the right answer.</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">FixFalseSharing</span> &#123;</span><br><span class="line">  <span class="built_in">alignas</span>(<span class="number">64</span>) <span class="keyword">volatile</span> <span class="type">uint64_t</span> x&#123;<span class="number">0</span>&#125;;</span><br><span class="line">  <span class="built_in">alignas</span>(<span class="number">64</span>) <span class="keyword">volatile</span> <span class="type">uint64_t</span> y&#123;<span class="number">0</span>&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>The padding happens during compilation phase by adding padding bit between the variables.</p><p><img src="/images/false_sharing_5.png"></p><h3 id="Method2-Thread-local"><a href="#Method2-Thread-local" class="headerlink" title="Method2: Thread local"></a>Method2: Thread local</h3><p>Another way to avoid false sharing is to use thread-local storage by rethinking the design if possible.<br>By declaring the variables as thread-local, each thread gets its own copy of the variable, which avoids false sharing.<br>It’s like:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">thread_local</span> <span class="type">uint64_t</span> x; <span class="comment">// each process has its own version</span></span><br></pre></td></tr></table></figure><h2 id="Caveats"><a href="#Caveats" class="headerlink" title="Caveats"></a>Caveats</h2><ul><li><p><strong>Padding wastes memory</strong>. A padded <code>char</code> occupies 64 bytes instead of 1, and for a large array of padded structs, that 8x memory bloat can hurt cache efficiency at a higher level than the false sharing you were fixing.</p></li><li><p><strong>64 bytes is the common cache line size on x86&#x2F;ARM today, but it isn’t universal</strong>. Prefer <code>std::hardware_destructive_interference_size</code> over hardcoding the number.</p></li><li><p><strong>True sharing is a different problem</strong>. If two threads genuinely need to access the same variable, padding does nothing, that’s a synchronisation problem, not a layout problem.</p></li><li><p>To <strong>confirm false sharing is actually the cause</strong>, run <code>perf stat -e cache-misses ./xxxx</code> on Linux. A suspiciously high cache miss rate under the parallel workload is usually the tell.</p></li><li><p><strong>False sharing happens at every cache level, but it hurts worst at L1</strong>. Each core’s L1 and L2 are private, so when two cores write to variables on the same cache line, the coherence protocol keeps invalidating those private copies. The reloading core usually finds a valid copy in the shared L3 (about 40 cycles) instead of going all the way to RAM, since the contested line stays hot in the hierarchy from the other core’s writes. The cost isn’t the fetch distance, it’s how often the invalidation happens: a 4-cycle L1 hit turns into a 40-cycle L3 reload on every single write. Across NUMA sockets with no shared L3, it gets worse still.</p></li></ul><h2 id="Key-takeaway"><a href="#Key-takeaway" class="headerlink" title="Key takeaway"></a>Key takeaway</h2><p><img src="/images/false_sharing_6.png"></p>]]></content>
      
      
      <categories>
          
          <category> C++ </category>
          
      </categories>
      
      
        <tags>
            
            <tag> performance </tag>
            
            <tag> C++ </tag>
            
            <tag> under-the-hood </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>What&#39;s true understanding?</title>
      <link href="/2026/02/02/what_true_understanding/"/>
      <url>/2026/02/02/what_true_understanding/</url>
      
        <content type="html"><![CDATA[<blockquote><p>Recently I’ve taken a class in UoA called “Self-Leadership”, which has given me a great deal of food for thought. It was somewhat novel and unfamiliar to me, and I mistook it for a typical self-success course.<br>But the course content turned out to be surprisingly insightful. I’ve learned the most fundamental practice of self-leadership: <strong>reflection</strong>. There is no secret formula or magic behind it. It simply offers me a quiet chance to pause, slow down, and reflect on my inner self.</p></blockquote><h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a><em>TL;DR</em></h2><ul><li>The illusion of understanding is a common trap in learning, where we feel like we understand something but lack genuine mastery.</li><li>Use Feynman’s Method to check if you understand the concept.</li></ul><h2 id="Observation"><a href="#Observation" class="headerlink" title="Observation"></a>Observation</h2><p>These days I spent plenty of time diving into <a href="https://csapp.cs.cmu.edu//">CSAPP</a>, a classic book in computer science. Over the past few years in my work, I always intended to read this book thoroughly, but never got around to it.</p><p>What I found interesting is that I acted differently. I somehow took on two distinct approaches once I began learning basic concepts such as <code>Virtual Memory</code> and <code>Set Associative Caches</code>.</p><p>To summarize, I understand <code>Set Associative Caches</code> better than <code>Virtual Memory</code>. Meanwhile, reading the chapter on <code>Virtual Memory</code> felt more enjoyable and rewarding. In contrast, studying the other topic is quite challenging and “full of dopamine”.</p><h2 id="Inflection"><a href="#Inflection" class="headerlink" title="Inflection"></a>Inflection</h2><p><strong>Why?</strong><br>It is an issue with the <strong>illusion of understanding</strong>. It is <strong>recognition</strong>, yet not genuine mastery.</p><p>When I closed the book, and looked back at the whole picture of <code>Virtual Memory</code>. I asked myself, but I got stuck over and over again. I couldn’t clarify the details of the concept, and I came up with some new questions. It ended up the knowledge is like a cyclone, it comes and goes, leaving a mess.</p><p>I think the reason is that I used to use <code>Virtual Memory</code> a lot in my work. Therefore, I just read the concepts happily without stop thinking more about it. There was a voice in mind said “Yeah, that’s it. The layout is just like that. The underlying mechanism is just like that.”. I was so satisfied that I had learnt so much <em>again</em>. (Maybe after several days, I could be “happy” once more). 😔</p><p>In terms of <code>Set Associative Caches</code>, it’s not a huge knowledge, so I break it down into smaller parts and draw diagrams to help me understand it better. I somehow map the diagrams in my mind, and start explaining it to myself. Every time I get stuck by something that I can’t make it clear, I just go back to the book and read it again. Eventually, I really master it! I can spot connections between it and my existing knowledge. 😁</p><h2 id="Implication-Action"><a href="#Implication-Action" class="headerlink" title="Implication &amp;&amp; Action"></a>Implication &amp;&amp; Action</h2><p>I may have tried these methods consciously or unconsciously before. But now I need to define them clearly and supervise myself to see if I can put them into practice 👊.</p><ol><li>Thinking about how to articulate the ideas without opening the book. Actually it’s <strong><a href="https://subjectguides.york.ac.uk/study-revision/feynman-technique">Feynman’s Method</a></strong>. Personally, I can understand about 90% of new knowledge after using the <code>Feynman Method</code>.</li><li><strong>Drawing diagrams</strong> to help me understand the concepts better.</li><li><strong>Break down</strong> the knowledge into smaller parts, making it easier to take action.</li></ol><h2 id="Attachment"><a href="#Attachment" class="headerlink" title="Attachment"></a>Attachment</h2><p>Here’s the Pros. and Cons. of using Feynman’s Method, just for you information.</p><p><img src="/images/feynman.png" alt="Feynman&#39;s Method"></p>]]></content>
      
      
      <categories>
          
          <category> Everyday Reflection </category>
          
      </categories>
      
      
    </entry>
    
    
  
  
</search>
